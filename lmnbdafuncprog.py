# lambda functions or anonymous functions are single liner functions which works as the same functions,
# that we define traditionally.

'''def plus(a,b):
    return a+b'''
plus = lambda a,b: a+b #this is same as def plus function.
print(plus(5,7))